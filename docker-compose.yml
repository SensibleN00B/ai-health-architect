services:
  app:
    build: .
    container_name: ai-health-architect
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/health.db
    restart: unless-stopped
